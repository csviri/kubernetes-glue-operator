apiVersion: io.csviri.operator.resourceflow/v1beta1
kind: ResourceFlow
metadata:
  name: "testresourceflow"
spec:
  resources:
    - name: cluster-scoped-resource
      resource:
        apiVersion: io.csviri.operator.resourceflow/v1
        kind: ClusterScopeTestCustomResource
        metadata:
          name: "test-resource-1"
        spec:
          value: "value1"

