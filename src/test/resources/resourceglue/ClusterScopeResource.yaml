apiVersion: io.csviri.operator.resourceglue/v1beta1
kind: Glue
metadata:
  name: "testresourceglue"
spec:
  resources:
    - name: cluster-scoped-resource
      resource:
        apiVersion: io.csviri.operator.resourceglue/v1
        kind: ClusterScopeTestCustomResource
        metadata:
          name: "test-resource-1"
        spec:
          value: "value1"

