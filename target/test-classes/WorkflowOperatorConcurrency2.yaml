apiVersion: io.csviri.operator.workflow/v1beta1
kind: WorkflowOperator
metadata:
  name: wo2
spec:
  parent:
    apiVersion: io.csviri.operator.workflow/v1
    kind: TestCustomResource2
  resources:
    - name: configMap1
      resource:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: "{{parent.metadata.name}}"
        data:
          key: "{{parent.spec.value}}"
